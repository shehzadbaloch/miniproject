<%= form_for(@include) do |f| %>
  <% if @include.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@include.errors.count, "error") %> prohibited this include from being saved:</h2>

      <ul>
      <% @include.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quality %><br>
    <%= f.text_field :quality %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.text_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.text_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
